我用的是BFS。

输入完毕后，可以构建一棵树。此时有两种数据结构来表示：
（1）常规的，Node节点，其中有一个Node数组来存放它的直接后代
（2）二维数组，而且是bool型，比如pedigree[i][j]为true时，表示i是j的直接父节点，否则两者毫无关系。这也是我所使用的。

然后就用BFS，从编号“01”（题目所说的根节点）开始探索。为什么用BFS呢？因为BFS是层级式搜索，恰好符合题目中所说的“输出每一层的叶子节点数”，所以就可以在BFS的过程中进行计数。

有1组测例没有过，不知为何。
