根据Output可知，要根据rank来对应输出不同的字符串。这里有一个“编号（string）-->rank（int）”的映射，所以可以使用map。然后根据不同的rank和相应的规则，适当安排if语句，比如把“是否checked”放在第一，然后再判断是不是存在这个编号，然后再判断素数什么的。这里涉及到if分支的安排。
总的来说，题目不难。

考点：
1、map的使用
2、素数判断
3、条件语句的顺序安排
