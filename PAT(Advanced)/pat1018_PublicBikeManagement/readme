题意：输出最短路径中符合某个条件的那条。
思路：
1、单源最短路径，可以用Dijkstra。记录路径可以使用：int last[N];而如果有多条路径，则在松弛的时候需要记录下来，用：vector<int> last[N];
2、输出的时候，如果是一条的话，可以简单的用一个while找出这条路径。但如果是多个，用while就不那么方便了，最好是用dfs，为什么呢？因为从终点追溯到起点，其实是形成了一颗树（终点为根，起点为叶子），要找出每一条路径，就可以对这棵树进行DFS。


