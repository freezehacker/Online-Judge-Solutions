题意：
	按照分数，求总排名和区排名。最后按照总排名的顺序，输出每个人的记录，包括id、总排名、区号、区排名。

思路：
	注意这里有两个排名：总排名、区排名。
	每个区输入完毕之后，我们可以sort一下，求出区排名了，用一个成员变量来维护。
	等所有输入完毕之后，又sort一下，求出总排名，也记录在每个Record的一个成员变量中。
	最后就按照顺序输出。

坑：
	同样的分数，排名是一样的，但是要按照id的字典序来输出。而如果有两个第一名，就没有第二名，下一个就直接是第三名了，这里需要注意。

