题意：根据中后序，输出层序（BFS）。
知识点：preOrder, postOrder, inOrder, levelOrder这4种遍历中，知道了inOrder和其他任意一个序列，就可以推出这个二叉树。中序是必须的。
解决方案：根据中后序，递归地构造出一个二叉树，然后对其进行BFS。
关键：构造二叉树的过程，是递归的。其实可以模拟我们人为的建树过程。
例子：
        B
       /  \
     A     C
      \   / \
      D  E   F
     /
    G

已知：
（1）前序：BADGCEF
（2）中序：AGDBECF

每一轮递归的方法是这样的：
（1）看前序，首先B肯定是当前子树的root了；
（2）在中序中找到B，然后就把中序序列分割成：AGD B ECF，即AGD是当前子树的左子树，而ECF是右子树；
（3）递归对AGD、ECF这两部分（两个子树）进行（1）（2）。
